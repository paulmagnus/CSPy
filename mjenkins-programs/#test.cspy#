def foo(x:int) -> bool:
    if x == 1:
        return True
    print("x is not 1")

# def bar(x:int) -> bool:
#     if x == 1:
#         return True
#     else:
#         print(x is not 1)  

print(foo(2))
